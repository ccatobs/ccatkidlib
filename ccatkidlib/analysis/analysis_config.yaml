---
file_paths:
  root_data_dir: '/mnt/md0/cooldown_june'
  g3_root_dir: '/mnt/md0/'
  fit_dir: '/home/jovyan/fitting'

io:
  file_level: 'CRITICAL'    # * File logging level
  terminal_level: 'CRITICAL' # * Terminal logging level 
  logging_dir: ""        # * Logging directory. Will create log in ccatkidlib/logs if ""

---
plot:
  width: 600
  height: 400

...