---
tones:
  max_tones: 1000 # * Max number of tones per data file

file_paths:
  original_root_data_dir: '/home/rfsoc/ccatkidlib_data'
  root_data_dir: '/home/rfsoc/ccatkidlib_data'
  original_g3_root_dir: '/home/rfsoc/ccatkidlib_data'
  g3_root_dir: '/home/rfsoc/ccatkidlib_data'
  fit_dir: '/home/jovyan/fitting'

io:
  file_level: 'CRITICAL' # * File logging level
  terminal_level: 'INFO' # * Terminal logging level 
  logging_dir: ""        # * Logging directory. Will create log in ccatkidlib/logs if ""

---
static_plot:
  sweep:
    aspect: 1
    fig_size: 100
    marker: 'o'
    marker_size: 3
    linewidth: 1
    tone_marker: '*'
    tone_marker_size: 3
  stream:
    aspect: 1
    fig_size: 100
    marker: 'o'
    marker_size: 3
    linewidth: 1
    datashade: True
save:
  save_fig: True # Whether to automatically save figures generated by plotting methods
  overwrite: False # Whether to overwrite figures with the same file name
  save_root_dir: '/mnt/md0/cooldown_october' # Root directory where figures should be saved
  save_fmt: 'png' # Format to save figure
  figs_per_file: 144 # Maximum number of figures per file

...